import Link from "next/link";

const PrimaryButton = () => {
    return (
        <Link href="/contact">
            <button>Primary CTA</button>
        </Link>
    );
};

export default PrimaryButton;